import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppEmprendedores_movil2',
  webDir: 'www'
};

export default config;
